{
  "name": "LollapaloozaGames",
  "version": "1.0.0",
  "description": "Empanada Arcade - Menú de Juegos",
  "scripts": {
    "dev": "live-server --host=localhost --port=3001 --open=/",
    "start": "live-server --host=localhost --port=3001 --open=/"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4",
    "gsap": "^3.14.2",
    "howler": "^2.2.4"
  }
}
